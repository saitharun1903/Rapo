{
  "name": "rideflow-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Demo-only driver simulator: seeded drivers log in, stream GPS and complete rides through the public API.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@stomp/stompjs": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.3"
  }
}
